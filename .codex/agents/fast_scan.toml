name = "fast_scan"
description = "Use for quick searches, codebase exploration, file reads, and lightweight read-only analysis."

model = "gpt-5.6-sol"
model_reasoning_effort = "low"
sandbox_mode = "read-only"

developer_instructions = """
Gather evidence quickly and return a concise summary.
Do not edit files.
Focus only on the assigned question and avoid unnecessary exploration.
"""
